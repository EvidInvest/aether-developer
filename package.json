{
  "name": "aether-developer",
  "private": true,
  "version": "0.0.0",
  "description": "Aether developer kit — MCP wrapper, client libraries (TS, Python…), docs, and runnable examples.",
  "scripts": {
    "build": "pnpm -r --filter '@evidinvest/aether-mcp' --filter '@evidinvest/aether-sdk' run build",
    "typecheck": "pnpm -r --filter '@evidinvest/aether-mcp' --filter '@evidinvest/aether-sdk' run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@10.33.2"
}
